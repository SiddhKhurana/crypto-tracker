import React from 'react';

const Header = () => (
  <div style={{ textAlign: 'center', marginBottom: '30px' }}>
    <h1>Crypto Tracker</h1>
    <p>Live prices & 24h data via Coingecko</p>
  </div>
);

export default Header;
